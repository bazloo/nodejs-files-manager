import { CommandProvider } from "./commandProvider";

export class CommandExecutor extends CommandProvider {
    
    constructor() {
        this.commands = {
            
        };
    }

    execute() {
        
    }
}